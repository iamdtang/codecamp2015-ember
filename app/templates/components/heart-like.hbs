{{#if liked}}
  <i class="fa fa-heart fa-lg liked"></i>
  <span class="liked">{{yield}}</span>
{{else}}
  <span class="cursor" {{action 'like'}}>
    <i class="fa fa-heart-o fa-lg"></i>
    {{yield}}
  </span>
{{/if}}
