<div class="row">
  <div class="col-sm-4">
    <ul>
      {{#each model as |artist|}}
        <li>
          {{#link-to 'artists.artist.songs' artist.id}}
            {{artist.artistName}}
          {{/link-to}}
        </li>
      {{/each}}
    </ul>
  </div>
  <div class="col-sm-8">
    {{outlet}}
  </div>
</div>
